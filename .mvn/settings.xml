<?xml version="1.0" encoding="UTF-8"?>
<settings xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xmlns="http://maven.apache.org/SETTINGS/1.2.0"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.2.0 https://maven.apache.org/xsd/settings-1.2.0.xsd">

  <localRepository>${user.home}/.m2/artanpg/repository</localRepository>

  <servers>
    <server>
      <id>maven-releases</id>
      <username>${env.NEXUS_MAVEN_USERNAME}</username>
      <password>${env.NEXUS_MAVEN_PASSWORD}</password>
    </server>
    <server>
      <id>maven-snapshots</id>
      <username>${env.NEXUS_MAVEN_USERNAME}</username>
      <password>${env.NEXUS_MAVEN_PASSWORD}</password>
    </server>
  </servers>

  <mirrors>
    <mirror>
      <id>maven-public</id>
      <mirrorOf>*</mirrorOf>
      <name>ArtanPG Nexus Server, Public Repository Group</name>
      <url>https://repo.artanpg.ir/repository/maven-public/</url>
    </mirror>
  </mirrors>
</settings>
