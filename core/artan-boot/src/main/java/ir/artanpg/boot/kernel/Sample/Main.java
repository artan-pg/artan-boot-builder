package ir.artanpg.boot.kernel.Sample;

import ir.artanpg.boot.kernel.eventbus.EventBus;
import ir.artanpg.boot.kernel.eventbus.SimpleEventBus;

import java.util.UUID;

public class Main {

	static void main() {
//		EventBus eventBus = new SimpleEventBus();

//		UserEntity userEntity = new UserEntity(UUID.randomUUID().toString(), "restless70", "pskPSK258##");
//		eventBus.publish(new UserCreatedEvent(userEntity));
	}
}
